import React from "react";
import { Link } from "@reach/router";

const Trip = props => {
  const { destination, tripType, departDate, returnDate, pasengerName, passengerGender, passengerBirthday, numPassengers, numBags } = props;

  return (
    //TODO
  );
};

export default Trip;